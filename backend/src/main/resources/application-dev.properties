# Activar con: --spring.profiles.active=dev  (o -Dspring.profiles.active=dev)
# Solo para desarrollo local — nunca activar en staging/producción.
spring.h2.console.enabled=true
spring.jpa.show-sql=true
