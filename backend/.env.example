# Required — generate a strong random Base64 string (min 256 bits)
# Example: openssl rand -base64 32
JWT_SECRET=replace-with-a-real-256bit-base64-secret

# Required — password used to create admin accounts via POST /auth/register-admin
ADMIN_SECRET=replace-with-a-strong-admin-password
